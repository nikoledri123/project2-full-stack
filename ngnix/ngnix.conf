server {
    listen 80;

    location /api/ {
        proxy_pass http://api:8000/;
    }

    location /static/ {
        alias /usr/share/nginx/html/static/;
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}